Perfect — let’s make this a **coding practice set** on operators in C++. You’ll need to actually write and run code to solve them:

---

### 🟢 Easy
1. Write a program that takes two integers as input and prints:
   - Their sum using `+`
   - Their difference using `-`
   - Their product using `*`
   - Their quotient using `/`

2. Write a program that checks if a given number is **even or odd** using the modulus operator `%`.

---

### 🟡 Medium
3. Demonstrate the difference between **prefix** and **postfix** increment operators by writing a program that:
   - Initializes `int x = 5`
   - Prints the result of `++x` and `x++`
   - Shows the final value of `x`

4. Write a program that uses **logical operators** (`&&`, `||`, `!`) to check:
   - If a number is between 10 and 20 (inclusive)
   - If a number is either divisible by 3 or 5
   - If a number is NOT equal to 100

---

### 🔴 Hard
5. Create a `Complex` class and overload the `+` operator so that you can add two complex numbers like this:
   ```cpp
   Complex c1(2, 3), c2(4, 5);
   Complex c3 = c1 + c2;
   c3.display(); // should print 6 + 8i
   ```

---

These are **hands-on coding problems** — you’ll need to implement and run them in your C++ environment (like the repo you have open on GitHub).  

👉 Do you want me to also give you **expected outputs** for each, so you can quickly check your solutions after coding?